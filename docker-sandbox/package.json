{
  "name": "docker-sandbox",
  "version": "1.0.0",
  "description": "Docker image for sandbox container",
  "main": "sandbox.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vm2": "^3.6.10"
  }
}
